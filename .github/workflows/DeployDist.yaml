name: Deploy Dist
on: [push, workflow_dispatch]
jobs:
  test:
    runs-on: mylocal
    steps:
      - name: Get code
        run: |
          echo "Cloning repo"
          echo "${{toJson(github)}}"
          git clone https://github.com/${{github.repository}}.git
